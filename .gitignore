# Data (never tracked)
data/
data/**
**/data/
**/data/**

# System files (declare once)
.DS_Store
.venv/

# Python bytecode
__pycache__/


archive/


# Vim and Stata swap/lock files
*.swp
# Stata dataset swap lock files
*.dta.swp
# Stata swap lock files (other)
*.stswp

# Local folders for old scripts
**/bin/

# Local git worktrees (not needed in repo)
worktrees/

# HPC generated files
hpc/sbatch/
hpc/out/

# Local logs and scratch outputs
log/
src/stata/log/
spec/stata/log/
spec/stata/archive/
spec/stata/scratch/
writeup/tex/build/
writeup/scratch/
writeup/py/scratch/
*.log
*.smcl
*.DS_Store

# LaTeX build artefacts
*.aux
*.fdb_latexmk
*.fls
*.synctex.gz
*.out

# Writeup build outputs (ignore broad; re-allow core source + final PDF)
writeup/tex/**/*.pdf
writeup/tex/**/*.tex
writeup/tex/**/*.aux
writeup/tex/**/*.log

# Allow primary sources and final deliverable
!writeup/tex/mini-writeup.tex
!writeup/tex/mini-writeup-2.tex
!writeup/tex/final/mini-writeup.pdf

# Generated analysis results (all derived; regenerate via specs + formatters)
results/
results/**

# Stata swap files anywhere
*.dta.swp
*.stswp
